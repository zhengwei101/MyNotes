# MySQL面试题

## 索引为什么要用B+树而不是二叉树或B树？

索引：

- 一种有序的存储结构
- 用来帮助数据库组织数据
- 提升搜索效率
- 映射的是磁盘数据
- 数据结构考察
  - 平衡二叉树
    - 为了避免树结构增删改而退化成单链表，通过平衡左右子树的高度，确保搜索时间复杂度稳定在O(logN)
    - 特征
      - 通过比较key来保证二叉树的有序
      - 一个节点存储的是kv对，一个节点存储的信息较少
  - B 树
  - B+ 树
- 选择标准
  - 降低磁盘io
  - 方便范围查询